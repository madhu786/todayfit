import React from "react";
import ReactDOM from "react-dom/client";
import TodayFit from "./TodayFit.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TodayFit />
  </React.StrictMode>
);
